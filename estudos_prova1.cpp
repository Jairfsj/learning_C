#include <iostream>
using namespace std;
int main()
{
   int i, num , qnt == 0, vet[10];
   cin>>num;
   for (i = 0; i < 10; i++) {
     if (vet[i] == num) {
       qnt++;
   }
   }
   {  
   cout << qnt;
   }
   
   system("pause");
   return(0);   

}
